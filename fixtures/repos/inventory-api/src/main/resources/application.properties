server.port=8083
server.servlet.context-path=/inventory-v1
spring.application.name=inventory-api

# Java 21 virtual threads (Loom) for the web container and task executor.
spring.threads.virtual.enabled=true

# Micrometer Tracing / OpenTelemetry sampling.
management.tracing.sampling.probability=1.0
