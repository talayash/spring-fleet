# Never commit a real environment config — it contains private paths, ports,
# service names, and trace keys. Use spring-fleet.config.example.json instead.
spring-fleet.config.json

# Local log collection dir (default)
.spring-fleet-logs/

# Python
__pycache__/
*.pyc

# OS / editor noise
.DS_Store
Thumbs.db
